import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fee',
    alignItems: 'center',
  },
  header: {
    fontSize: 36,
    alignItems: 'center',
    backgroundColor: 'blue',
  

  },
});